use crate::FlashParser;
use crate::arithematics::{
    u32_length,
    u64_length,
    div10u32,
    div10u64,
};

impl FlashParser {
    /*
    pub fn u32_to_str(&mut self, u: u32) -> &[u8] {
        let len = u32_length(u);
    } 
    */  
}